{
  "name": "mmtags",
  "version": "1.1.2",
  "description": "analyze mm tags for given urls",
  "main": "./server/dist/main.js",
  "bin": {
    "versatag": "./server/dist/main.js"
  },
  "scripts": {
    "test": "node .",
    "start": "npm install && concurrently \"cd app && npm install && npm start\" \"cd server && npm install && npm run start:dev\""
  },
  "files": [
    "server"
  ],
  "author": "Brandon Gates",
  "license": "MIT",
  "dependencies": {
    "@nestjs/common": "^6.0.0",
    "@nestjs/core": "^6.0.0",
    "@nestjs/platform-express": "^6.0.0",
    "puppeteer": "^1.18.1",
    "reflect-metadata": "^0.1.12",
    "rimraf": "^2.6.2",
    "rxjs": "^6.3.3"
  },
  "devDependencies": {
    "concurrently": "^4.1.1"
  }
}
